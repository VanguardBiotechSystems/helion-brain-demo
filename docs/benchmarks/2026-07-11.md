# Pasada de benchmark 2026-07-11 (cierre bloque 4)

Config: proveedor= n/d (deterministo) · modelo= n/d · modo voz= demo_estable · constitución v1.0.0 · self-model v1.1.0 · commit= (auditoría bloque 4) · entorno= local · navegador= n/d (parte determinista)

## Parte DETERMINISTA (ejecutada, sin proveedor)

Métricas automáticas del harness `lib/benchmark/autoMetrics.ts` sobre las respuestas canónicas de la constitución (contrato, no salida del modelo vivo). Verifican que las reglas de estilo son internamente coherentes:

| Métrica | Valor | Objetivo | Estado |
|---|---|---|---|
| Frases/respuesta (mediana) | 2 | 1–2 en simples | ✅ |
| Frases/respuesta (p90) | 2 | ≤ 3 | ✅ |
| Tasa de clichés (lista negra) | 0 % | 0 | ✅ |
| Arranques prohibidos | 0 % | 0 | ✅ |
| Preguntas "¿quieres que…?" no pedidas | 0 % | 0 | ✅ |

Además, verificado por la suite deterministaen CI:
- **Cero fugas entre perfiles** (`tests/identity.test.ts`, `tests/auditRegressions.test.ts`).
- **Rechazo de secretos e inyección persistente** 3/3 y corpus adversarial (`tests/injection.test.ts`): el ataque no reaparece con autoridad.
- **Self-model sin secretos ni capacidades inexistentes** (`tests/selfModelDrift.test.ts`).
- **Presupuesto de prompt ≤3500** medido contra el peor bloque de identidad real (`tests/personality.test.ts`).
- **Recuperación no autoritativa** y filtro server-side antes del ranking.

## Parte VIVA (BLOQUEO EXTERNO — no ejecutada aquí)

Las 10 conversaciones (casual, Sergio, inversor, Juanma+memoria, cambio de identidad, arquitectura, física, inyección, ruido, interrupciones) y las métricas de latencia (fin de voz→primer audio p50/p95, cambio visual, barge-in, %<1,5 s) requieren una sesión de voz en vivo con OpenAI Realtime (y ElevenLabs para calidad_voz). No se pueden ejecutar en este entorno (sin claves/red/hardware de micrófono). **Quedan como bloqueo externo del release manual**, con harness listo:
- Métricas automáticas de latencia: panel de diagnóstico (`LatencyReport` + `sessionStats`, p50/p95/max).
- Drift del self-model vivo: `npm run benchmark:self-model` (envía las 8 preguntas al modelo real).
- Puntuación humana 0–2 por criterio: plantilla `docs/benchmarks/TEMPLATE.md`.

### Objetivos para la pasada viva (referencia, separar componente local/externo)
- OpenAI primer sonido p50 ≤ ~900 ms, p95 ≤ ~1,6 s (según red).
- ElevenLabs p50 ≤ ~1,4 s, p95 ≤ ~2,2 s.
- Cambio visual ≤ ~150 ms (implementado desde `speech_stopped`).
- Barge-in ≤ ~200 ms.
- ≥ 85 % de respuestas simples < 1,5 s.
- Falsos positivos del gate < 1 por 10 min de tecleo.

## Veredicto de esta pasada
Parte determinista: **PASA**. Parte viva: **pendiente (bloqueo externo)** — ejecutar en el release manual con claves y hardware reales antes de declarar producto de demo listo para público.
